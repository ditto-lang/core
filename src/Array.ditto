module Array exports (index);

import Maybe (Maybe(..));

index (i : Int) (arr : Array a) : Maybe a =
  index_impl Just Nothing i arr;

foreign index_impl (yep : a -> Maybe a) (nope : Maybe a) (i : Int) (arr : Array a) : Maybe a;
