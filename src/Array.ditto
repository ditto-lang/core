module Array exports (..);

import Maybe (Maybe(..));

index (i : Int) (arr : Array a) : Maybe a =
  index_impl Nothing Just i arr;

foreign index_impl 
  (nope : Maybe a) 
  (yep : a -> Maybe a) 
  (i : Int) 
  (arr : Array a) 
  : Maybe a;
